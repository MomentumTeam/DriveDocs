exports.convertAndUpdateInDrive = async (fileId, newFormat, driveAccessToken) => {
  // const fs = require('fs');
  // const request = require('request');
  // let req = request.post('http://localhost:3000/convert');
  // let form = req.form();
  // form.append('file', fs.createReadStream('video.mp4'));
  // form.append('format', 'gif');
  // req.pipe(fs.createWriteStream('image.gif'));
  //TODO Call the api of Versed in order to convert the file
  //TODO update in drive according to the new converted file with drive.updateFile()
};
